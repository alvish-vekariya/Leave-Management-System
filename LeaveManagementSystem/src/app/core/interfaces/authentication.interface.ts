export interface IsignupData {
    _id?: string,
    username ?: string,
    dob ?: Date,
    email ?: string,
    password ?: string,
    gender ?: string,
    role ?: string
}

export interface IloginData{
    username : string,
    password : string
}